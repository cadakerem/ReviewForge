name: ReviewForge SecOps
on: [pull_request, push]

jobs:
  ai_review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      
      - name: Run ReviewForge AI
        uses: ./
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          ai_provider: "groq" 
          ai_api_key: ${{ secrets.GROQ_API_KEY }}
          deep_model: "llama3-70b-8192"
          light_model: "llama3-70b-8192"
